const cardContainer = document.querySelector('.js-card-container');
const searchInput = document.querySelector('.js-input');
const form = document.querySelector('.search-form');

export { cardContainer, searchInput, form };
